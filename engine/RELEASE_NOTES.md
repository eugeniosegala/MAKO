## What's new in MAKO Renderer v2.0.0

MAKO Renderer 2.0 is the complete frame-generation foundation behind the new MAKO experience: adaptive, responsive, isolated, and built for real SteamOS gameplay rather than ideal laboratory conditions.

- **Adaptive Frame Generation that follows the game:** Choose a 30–240 FPS target and a 2x–4x ceiling while MAKO continuously adjusts generated-frame count around real performance. Smooth Cadence and the optional Adaptive FPS Cap help stabilise uneven delivery without pretending every target is always reachable.
- **Live control with safe GPU boundaries:** Frame Generation on/off, Fixed and Adaptive modes, supported multiplier changes, target FPS, cadence, and FPS caps can update while a game is running. Resource-shape and model changes remain restart-only so live tuning never trades responsiveness for swapchain instability.
- **Resilience through menus, overlays, hitches, and load spikes:** The scheduler distinguishes gameplay from short interruptions, sheds generated work when the GPU or compositor is saturated, keeps real frames presenting when generation is temporarily unavailable, and warms temporal history before resuming.
- **Process- and context-safe isolation:** MAKO uses its own opt-in Vulkan identity, explicit wrapper activation, process-profile matching, and context-specific diagnostics. Concurrent or replacement render contexts stay separated, while games not launched through MAKO remain untouched.
- **Fixed and Adaptive tuning in one standalone UI:** The desktop configuration app now exposes Fixed 2x–4x, Adaptive controls, Base FPS Cap, Adaptive FPS Cap, Flow Scale, FP16, Performance Mode, profile selection, and model settings in a scrollable interface that remains usable on smaller displays.
- **Native, Flatpak, and multi-architecture coverage:** Every host release includes genuine 64-bit and 32-bit Vulkan layers. Freedesktop 23.08, 24.08, and 25.08 extensions carry both architectures for Heroic and other Flatpak launch paths, with portable-home handling and validated GPU selection for multi-GPU systems.
- **A complete Linux installation experience:** MAKO installs its uniquely named manifests, CLI, Qt configuration UI, desktop entry, and launcher assets without bundling or modifying `Lossless.dll`. Start it from **MAKO Renderer Configuration** in Desktop Mode or with `~/.local/bin/mako-ui`.
- **Release-grade validation:** Deterministic scheduler and recovery tests cover native-only output, interpolation ceilings, cadence, load shedding, transitions, hitches, and recovery. Packaging verifies architecture, archive layout, Flatpak contents, checksums, source commits, and Decky pins before publication.

## What's new in MAKO Renderer v1.1.2

This release makes the standalone MAKO Renderer experience more complete and easier to navigate, especially for users who configure games outside Decky.

- **Control both FPS caps from the desktop UI:** Base FPS Cap and Adaptive FPS Cap are now available alongside the existing Fixed, Adaptive, quality, and performance controls. The manual cap clearly shows when it is off and becomes unavailable while Adaptive's automatic cap is active.
- **A settings layout that stays usable:** The configuration pane now scrolls cleanly, keeping every profile and renderer option reachable on smaller displays as the interface grows.
- **A clearer boundary between Renderer and Decky:** The UI explains that automatic game discovery, launcher compatibility, Flatpak preparation, and installation management remain part of MAKO Decky, while native renderer settings stay available in `mako-ui`.
- **Configuration is easier to find and start:** After extracting the host archive, open **MAKO Renderer Configuration** from the application menu or run `~/.local/bin/mako-ui` from a terminal. The installation guide and generated release instructions now show both paths directly.
